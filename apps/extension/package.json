{
  "name": "@promolens/extension",
  "version": "0.1.0",
  "private": true,
  "description": "PromoLens Chrome extension (Manifest V3).",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@promolens/shared": "*"
  }
}
